# Copy to .env.relayer-db.local. Keep this file readable only by the deploy
# operator. The URL in .env.relayer.local must use the same three values.
POSTGRES_DB=REPLACE_RELAYER_DB_NAME
POSTGRES_USER=REPLACE_RELAYER_DB_USER
POSTGRES_PASSWORD=REPLACE_URL_SAFE_RELAYER_DB_PASSWORD
